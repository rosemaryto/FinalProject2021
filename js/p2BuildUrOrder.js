/* -------------------------------------------BUILD ORDER - DOUGH OPTIONS---------------------------------------------- */

document.getElementById("addressType").addEventListener('change', function (event) {
    if (event.target && event.target.matches("input[name='handTossed']")) {
      //CREATE SELECT
        
    } else if (event.target && event.target.matches("input[name='thinCrust']")) {
      //CREATE SELECT

    } else if (event.target && event.target.matches("input[name='nyStyle']")) {
      //CREATE SELECT
    
    } else {
    //CREATE SELECT

  }
  });
  
  
  
  
  
  
  
  
  
  
  
  